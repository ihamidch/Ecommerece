# Frontend production API base URL (must end with /api)
# Example (Vercel): https://backend-two-weld-46.vercel.app/api
VITE_API_URL=

# Note: Cloudinary credentials are configured on the backend only.
# This app does not use a single CLOUDINARY_URL string; see backend/.env.example.
